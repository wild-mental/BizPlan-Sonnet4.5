# AI 심사위원단 평가 데모 구현 제안서

## 1. 개요

### 1.1 목적
랜딩 페이지의 "심사받기" 관련 버튼 클릭 시 사용자가 M.A.K.E.R.S AI 심사위원단의 평가 기능을 직접 체험할 수 있는 데모 페이지를 구현합니다.

### 1.2 관련 버튼 (현재 상태)
| 위치 | 버튼 문구 | 현재 동작 | 변경 후 동작 |
|------|----------|----------|-------------|
| Hero Section | "지금 바로 심사받기" | `#makers-section` 스크롤 | `/evaluation-demo` 이동 |
| 기본 요금제 카드 | "AI 평가받기 데모" | `/demo/evaluation` 이동 | `/evaluation-demo` 이동 |
| Makers Section | "지금 바로 평가받기" | 프로모션 모달 | `/evaluation-demo` 이동 |

---

## 2. 핵심 기능 정의

### 2.1 M.A.K.E.R.S 6대 평가 영역

| 코드 | 영역명 | 한글명 | 주요 평가 항목 |
|------|--------|--------|---------------|
| M | Marketability | 시장성 | TAM/SAM/SOM, 경쟁사 분석, 타깃 고객 세분화 |
| A | Ability | 수행능력 | 팀 구성, 창업자 역량, 실행 가능성 |
| K | Key Technology | 핵심기술 | 기술 혁신성, 차별화, 지식재산권 |
| E | Economics | 경제성 | 매출/손익 계획, 자금 조달, 재무 건전성 |
| R | Realization | 실현가능성 | 사업화 전략, 로드맵, 마일스톤 |
| S | Social Impact | 사회적가치 | ESG, 일자리 창출, 지역사회 기여 |

### 2.2 데모 체험 범위 (무료 vs 유료)

| 기능 | 무료 데모 | 플러스 | 프로 | 프리미엄 |
|------|----------|--------|------|---------|
| 6대 영역 점수 확인 | ✅ (1회) | ✅ 무제한 | ✅ 무제한 | ✅ 무제한 |
| 영역별 상세 피드백 | ❌ 요약만 | ✅ 전체 | ✅ 전체 | ✅ 전체 |
| 개선 제안 | ❌ | ✅ 기본 | ✅ 상세 | ✅ 전문가급 |
| 재작성 루프 | ❌ | ❌ | ✅ 80점 미달 시 | ✅ 무제한 |
| PDF 리포트 다운로드 | ❌ | ✅ | ✅ | ✅ |

---

## 3. 페이지 구조 설계

### 3.1 라우트 구성

```
/evaluation-demo           # 메인 평가 데모 페이지
/evaluation-demo/input     # 사업계획서 입력 단계
/evaluation-demo/analyzing # 분석 중 애니메이션
/evaluation-demo/result    # 평가 결과 확인
```

### 3.2 사용자 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│  1. 랜딩 페이지                                                   │
│     └─> "심사받기" 버튼 클릭                                      │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. 평가 데모 소개 (/evaluation-demo)                             │
│     ├─ M.A.K.E.R.S 6대 평가 영역 설명                             │
│     ├─ 데모 체험 범위 안내 (무료 한계 명시)                        │
│     └─> "평가 시작하기" 버튼 클릭                                  │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. 사업계획서 입력 (/evaluation-demo/input)                      │
│     ├─ 샘플 템플릿 선택 또는 직접 입력                             │
│     ├─ 핵심 영역별 간략 질문 (각 2-3개)                           │
│     │   ├─ [M] 시장성: 타깃 고객, 시장 규모                        │
│     │   ├─ [A] 수행능력: 팀 구성, 핵심 역량                        │
│     │   ├─ [K] 핵심기술: 기술 차별점, IP 보유 여부                  │
│     │   ├─ [E] 경제성: 예상 매출, 손익분기점                       │
│     │   ├─ [R] 실현가능성: 사업화 일정, 주요 마일스톤               │
│     │   └─ [S] 사회적가치: 일자리 창출, ESG 기여                   │
│     └─> "AI 심사 시작" 버튼 클릭                                   │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. 분석 중 애니메이션 (/evaluation-demo/analyzing)               │
│     ├─ 6명의 AI 심사위원 아바타가 순차 등장                        │
│     ├─ 각 심사위원이 해당 영역 분석 중 표시                        │
│     ├─ 진행률 바 + 예상 소요 시간                                  │
│     └─ (약 5-10초 소요)                                           │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. 평가 결과 (/evaluation-demo/result)                          │
│     ├─ 종합 점수 (100점 만점)                                     │
│     ├─ 6대 영역별 점수 (레이더 차트)                               │
│     ├─ 강점/약점 요약 (무료: 2개씩, 유료: 전체)                    │
│     ├─ 개선 권장사항 (무료: 1개, 유료: 전체)                       │
│     ├─ 예상 합격 확률 표시                                        │
│     └─ CTA: "상세 피드백 받기" → 요금제 페이지 유도                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. UI/UX 디자인 가이드

### 4.1 비주얼 컨셉
- **톤앤매너**: 기존 랜딩페이지와 동일한 다크 테마 + 그라데이션
- **심사위원 아바타**: `/assets/juror-single/` 이미지 활용
- **애니메이션**: 심사 진행 시 각 위원별 분석 애니메이션

### 4.2 핵심 UI 컴포넌트

```
src/
├── pages/
│   └── EvaluationDemo/
│       ├── index.tsx              # 메인 진입점
│       ├── IntroSection.tsx       # 소개 및 시작
│       ├── InputSection.tsx       # 사업계획서 입력
│       ├── AnalyzingSection.tsx   # 분석 중 애니메이션
│       └── ResultSection.tsx      # 결과 표시
├── components/
│   └── evaluation/
│       ├── JurorCard.tsx          # 심사위원 카드
│       ├── ScoreRadarChart.tsx    # 레이더 차트 (Recharts)
│       ├── ScoreGauge.tsx         # 점수 게이지
│       ├── FeedbackCard.tsx       # 피드백 카드
│       └── ProgressIndicator.tsx  # 진행 상태 표시
└── hooks/
    └── useEvaluationDemo.ts       # 평가 로직 훅
```

### 4.3 결과 화면 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│                    M.A.K.E.R.S AI 심사 결과                       │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐    ┌────────────────────────────────────┐ │
│  │   종합 점수       │    │       6대 영역 레이더 차트          │ │
│  │                  │    │                                    │ │
│  │      78점        │    │         M(85)                      │ │
│  │   ───────────    │    │      S    A(72)                    │ │
│  │    합격 가능성    │    │       ↖ ↗                          │ │
│  │      72%         │    │    R ←   → K                       │ │
│  │                  │    │       ↙ ↘                          │ │
│  └──────────────────┘    │      E(80)                         │ │
│                          └────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│  📊 영역별 상세 점수                                              │
│  ┌─────┬─────┬─────┬─────┬─────┬─────┐                          │
│  │  M  │  A  │  K  │  E  │  R  │  S  │                          │
│  │ 85  │ 72  │ 78  │ 80  │ 65  │ 88  │                          │
│  └─────┴─────┴─────┴─────┴─────┴─────┘                          │
├─────────────────────────────────────────────────────────────────┤
│  ✅ 강점 (2개 표시, 나머지 블러 처리)                              │
│  ├─ 시장성(M): TAM 분석이 명확하고 타깃 고객 정의가 우수함          │
│  └─ 사회적가치(S): ESG 기여도가 높고 일자리 창출 계획이 구체적      │
│                                                                 │
│  ⚠️ 개선 필요 (1개 표시, 나머지 블러 처리)                         │
│  └─ 실현가능성(R): 마일스톤이 불명확하고 리스크 대응 계획 부족       │
│                                                                 │
│  🔒 상세 피드백은 유료 요금제에서 확인 가능합니다                   │
├─────────────────────────────────────────────────────────────────┤
│  [상세 피드백 받기 →]  [요금제 보기]  [다시 평가하기]              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 기술 구현 사항

### 5.1 프론트엔드 (React + TypeScript)

#### 5.1.1 상태 관리 (Zustand)

```typescript
// src/stores/useEvaluationStore.ts
interface EvaluationState {
  // 입력 데이터
  businessPlanInput: BusinessPlanInput;
  
  // 평가 상태
  isEvaluating: boolean;
  currentStep: 'intro' | 'input' | 'analyzing' | 'result';
  analyzingProgress: number;
  currentJuror: string; // M, A, K, E, R, S
  
  // 결과 데이터
  evaluationResult: EvaluationResult | null;
  
  // 액션
  setInput: (input: Partial<BusinessPlanInput>) => void;
  startEvaluation: () => Promise<void>;
  resetEvaluation: () => void;
}
```

#### 5.1.2 평가 결과 타입

```typescript
// src/types/evaluation.ts
interface EvaluationResult {
  totalScore: number;          // 0-100
  passRate: number;            // 합격 확률 (%)
  scores: {
    M: number; A: number; K: number;
    E: number; R: number; S: number;
  };
  strengths: Feedback[];       // 강점
  weaknesses: Feedback[];      // 약점
  recommendations: string[];   // 개선 권장사항
  generatedAt: Date;
}

interface Feedback {
  area: 'M' | 'A' | 'K' | 'E' | 'R' | 'S';
  title: string;
  description: string;
  isBlurred: boolean;  // 무료 사용자는 일부 블러 처리
}
```

#### 5.1.3 레이더 차트 (Recharts)

```typescript
// src/components/evaluation/ScoreRadarChart.tsx
import { RadarChart, PolarGrid, PolarAngleAxis, Radar, ResponsiveContainer } from 'recharts';

const ScoreRadarChart: React.FC<{ scores: EvaluationResult['scores'] }> = ({ scores }) => {
  const data = [
    { subject: '시장성', score: scores.M, fullMark: 100 },
    { subject: '수행능력', score: scores.A, fullMark: 100 },
    { subject: '핵심기술', score: scores.K, fullMark: 100 },
    { subject: '경제성', score: scores.E, fullMark: 100 },
    { subject: '실현가능성', score: scores.R, fullMark: 100 },
    { subject: '사회적가치', score: scores.S, fullMark: 100 },
  ];

  return (
    <ResponsiveContainer width="100%" height={300}>
      <RadarChart data={data}>
        <PolarGrid stroke="rgba(255,255,255,0.2)" />
        <PolarAngleAxis dataKey="subject" tick={{ fill: 'white', fontSize: 12 }} />
        <Radar
          dataKey="score"
          stroke="#10b981"
          fill="#10b981"
          fillOpacity={0.3}
        />
      </RadarChart>
    </ResponsiveContainer>
  );
};
```

### 5.2 백엔드 API (향후 연동)

#### 5.2.1 엔드포인트 설계

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/v1/evaluation/demo` | 데모 평가 요청 |
| GET | `/api/v1/evaluation/demo/{id}` | 평가 결과 조회 |
| POST | `/api/v1/evaluation/full` | 전체 평가 요청 (유료) |

#### 5.2.2 요청/응답 예시

```json
// POST /api/v1/evaluation/demo
// Request
{
  "businessPlan": {
    "marketability": {
      "targetCustomer": "20-30대 1인 창업자",
      "marketSize": "연간 5조원 규모"
    },
    "ability": {
      "teamSize": 3,
      "coreCompetency": "AI 개발 경력 10년"
    },
    // ... 나머지 영역
  },
  "templateType": "예비창업패키지"
}

// Response
{
  "id": "eval_abc123",
  "totalScore": 78,
  "passRate": 72,
  "scores": { "M": 85, "A": 72, "K": 78, "E": 80, "R": 65, "S": 88 },
  "strengths": [
    { "area": "M", "title": "시장 분석 우수", "description": "TAM 분석이 명확함" }
  ],
  "weaknesses": [
    { "area": "R", "title": "마일스톤 불명확", "description": "..." }
  ],
  "isDemo": true,
  "fullResultsLocked": true
}
```

### 5.3 데모 로직 (프론트엔드 시뮬레이션)

초기 MVP에서는 백엔드 없이 프론트엔드에서 시뮬레이션 로직으로 데모 제공:

```typescript
// src/utils/evaluationSimulator.ts

export const simulateEvaluation = async (
  input: BusinessPlanInput
): Promise<EvaluationResult> => {
  // 입력 완성도 기반 점수 산출
  const scores = {
    M: calculateMarketabilityScore(input.marketability),
    A: calculateAbilityScore(input.ability),
    K: calculateKeyTechScore(input.keyTechnology),
    E: calculateEconomicsScore(input.economics),
    R: calculateRealizationScore(input.realization),
    S: calculateSocialValueScore(input.socialValue),
  };
  
  const totalScore = Object.values(scores).reduce((a, b) => a + b, 0) / 6;
  const passRate = calculatePassRate(totalScore, scores);
  
  return {
    totalScore: Math.round(totalScore),
    passRate,
    scores,
    strengths: generateStrengths(scores),
    weaknesses: generateWeaknesses(scores),
    recommendations: generateRecommendations(scores),
    generatedAt: new Date(),
  };
};
```

---

## 6. 구현 우선순위 및 일정

### Phase 1: 기본 데모 페이지 (2-3일)
- [ ] 라우트 설정 (`/evaluation-demo`)
- [ ] 소개 페이지 UI
- [ ] 간략 입력 폼 (6개 영역 × 2개 질문)
- [ ] 분석 중 애니메이션

### Phase 2: 결과 화면 (2-3일)
- [ ] 종합 점수 게이지
- [ ] 레이더 차트 (Recharts)
- [ ] 강점/약점 카드 (일부 블러)
- [ ] CTA 버튼 (요금제 유도)

### Phase 3: 시뮬레이션 로직 (1-2일)
- [ ] 점수 산출 알고리즘
- [ ] 피드백 생성 로직
- [ ] 합격 확률 계산

### Phase 4: 연동 및 최적화 (1-2일)
- [ ] 랜딩페이지 버튼 연결
- [ ] 반응형 최적화
- [ ] 애니메이션 튜닝

---

## 7. AI 에이전트 프롬프트 예시

### 7.1 페이지 생성 프롬프트

```
다음 요구사항에 맞게 AI 심사위원단 평가 데모 페이지를 생성해줘:

1. 경로: /evaluation-demo
2. 단계: 소개 → 입력 → 분석 중 → 결과
3. M.A.K.E.R.S 6대 평가 영역별 간략 질문 폼 구현
4. 분석 중 화면에서 6명의 심사위원 아바타가 순차 등장하는 애니메이션
5. 결과 화면에 Recharts 레이더 차트로 6대 영역 점수 시각화
6. 무료 데모이므로 상세 피드백 일부는 블러 처리하고 CTA 유도
7. 기존 랜딩페이지의 다크 테마 + 그라데이션 스타일 유지
8. 심사위원 이미지는 /assets/juror-single/ 경로 활용
```

### 7.2 시뮬레이션 로직 프롬프트

```
M.A.K.E.R.S 평가 시뮬레이션 로직을 구현해줘:

1. 사용자 입력 완성도 기반 점수 산출 (각 영역 0-100점)
2. 총점 평균 및 합격 확률 계산
3. 점수 기반 강점/약점 2개씩 자동 선정
4. 개선 권장사항 생성
5. 실제 정부지원사업 심사 기준 반영
6. 예비창업패키지, 초기창업패키지 템플릿별 가중치 차등 적용
```

---

## 8. 성공 지표 (KPI)

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 데모 시작률 | 버튼 클릭 후 입력 시작 60%+ | GA4 이벤트 |
| 데모 완료율 | 입력 완료 후 결과 확인 40%+ | GA4 퍼널 |
| 요금제 전환율 | 결과 확인 후 요금제 클릭 15%+ | GA4 전환 |
| 사전 등록 전환율 | 데모 후 사전 등록 10%+ | 서버 데이터 |

---

## 9. 향후 확장 계획

1. **LLM 연동**: 실제 AI 분석으로 고도화 (Gemini API)
2. **히스토리 저장**: 사용자별 평가 이력 관리
3. **비교 분석**: 이전 평가와 개선도 비교
4. **PDF 리포트**: 전문가급 보고서 자동 생성
5. **실시간 피드백**: 입력 중 실시간 점수 변화 표시

---

*문서 작성일: 2025년 12월 28일*
*작성자: AI Assistant*

